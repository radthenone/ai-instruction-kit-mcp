interface:
  display_name: "Ai Instruction Kit Mcp"
  short_description: "Repo-specific patterns and workflows for ai-instruction-kit-mcp"
  default_prompt: "Use the ai-instruction-kit-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true