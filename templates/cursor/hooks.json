{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "bash --noprofile --norc .cursor/hooks/gate-destructive.sh",
        "matcher": "git\\s+(push|reset|clean|commit)|\\brm\\s+-[a-zA-Z]*r",
        "failClosed": true
      },
      {
        "command": "bash --noprofile --norc .cursor/hooks/gate-push.sh",
        "matcher": "git\\s+push"
      }
    ]
  }
}
