# Profil projektu (opcjonalny — tylko fork kategorii)
# Zalecane: mcp.json z --preset _base|shop|… --workspace ${workspaceFolder}
# i ewentualnie samo .ai/project.md jako overlay.
# Ten plik twórz wyłącznie gdy nadpisujesz capabilities/domains/decisions.

name: my-project
language: pl

extends: profiles/_base.yaml

overlays:
  - .ai/project.md
