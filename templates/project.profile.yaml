# Profil projektu (opcjonalny — tylko fork presetu)
# Zalecane: mcp.json z --preset _base|olivin-app|… --workspace ${workspaceFolder}
# i ewentualnie samo .ai/project.md jako overlay.
# Ten plik twórz wyłącznie gdy nadpisujesz capabilities/domains/decisions.
name: my-project
language: pl

extends: profiles/_base.yaml

overlays:
  - .ai/project.md
