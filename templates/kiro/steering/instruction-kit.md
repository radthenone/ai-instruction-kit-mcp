---
inclusion: always
---

# Instruction-kit (Kiro steering)

Przestrzegaj `AGENTS.md` w root repo.

Przed pracą: MCP `project-guides` → `get_bundle` + `get_overlay` (+ `get_language` / `get_clients`).

Chronione branche: `main` / `master` / `dev` — tylko przez PR.
Hooki Cursor (`gate-*`) nie działają w Kiro; użyj git `pre-push` z kita oraz review przed pushem.
