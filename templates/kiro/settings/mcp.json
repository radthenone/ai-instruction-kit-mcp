{
  "mcpServers": {
    "project-guides": {
      "command": "uvx",
      "args": [
        "--from", "git+https://github.com/TWOJ_USER/ai-instruction-kit-mcp.git",
        "guides-mcp",
        "--preset", "_base",
        "--language", "pl",
        "--codegen", "orval",
        "--clients", "all",
        "--workspace", "${workspaceFolder}"
      ]
    }
  }
}
