# Frontend — React web only

`--frontend react` bez `--mobile`.

```text
frontend/
  src/
    app/                 # routing (np. React Router)
    core/
    features/<feature>/
    api/generated/
  package.json
  vite.config.ts         # lub równoważne
```

## Powiązane

- `arch:testing`, `arch:api-errors`
