# Frontend — Expo web only

`--frontend expo` bez `--mobile`.

```text
frontend/
  app/                 # Expo Router (web)
  src/
    core/
    features/<feature>/
    api/generated/
  package.json
```

## Powiązane

- `stack:expo-router:testing`, `arch:ui-ux-expo`
