# Frontend — React web + React Native (split)

`--frontend react` + `--mobile react-native`.

```text
frontend/
  web/
    src/
      features/<feature>/
  mobile/                # RN CLI / Expo bare — bez zakładania Expo Router
    src/
      features/<feature>/
  packages/
    <package>/
```

## Powiązane

- `arch:platforms`, `arch:testing`
